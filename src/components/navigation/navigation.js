import React from 'react';
import './navigation.css';

export default function Navigation(props) {
	return (
		<nav>
			<button>Jam</button>
		    <button>Jam Schedule</button>
		    <button>Start a Jam</button>
		    <button>Logout</button>
		</nav>
	);
}